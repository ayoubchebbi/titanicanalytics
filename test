<apex:page controller="PicklistController">
    <h2>اختيار متعدد في Picklist</h2>
    <apex:form >
        <apex:outputLabel value="اختر القيم:" />
        <select multiple="multiple" size="5" style="width: 200px;" id="picklist" >
            <apex:repeat value="{!options}" var="option">
                <option value="{!option.value}">{!option.label}</option>
            </apex:repeat>
        </select>
        <br/><br/>
        <apex:commandButton value="Submit" action="{!processSelectedValues}" />
        <br/><br/>
        <apex:outputText value="القيم المختارة: {!selectedValues}" />
    </apex:form>
</apex:page>
