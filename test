SELECT Id, JobType, Status, NumberOfErrors, JobItemsProcessed, 
       TotalJobItems, CreatedDate, CompletedDate, 
       ApexClass.Name, ExtendedStatus
FROM AsyncApexJob
WHERE JobType IN ('BatchApex', 'BatchApexWorker')
AND CreatedDate = 2024-10-10
ORDER BY CreatedDate DESC
