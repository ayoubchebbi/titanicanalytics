CASE(Time_Unit__c,
  "Minutes", Date_Start__c + (Value__c / 1440),
  "Hours", Date_Start__c + (Value__c / 24),
  "Days", Date_Start__c + Value__c,
  "Weeks", Date_Start__c + (Value__c * 7),
  "Months", ADDMONTHS(Date_Start__c, Value__c),
  Date_Start__c
)
