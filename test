this.patientsFromPolygon = [];
((data)?.uiapi?.query?.Address?.edges || []).forEach(item => {
  const id = item?.node?.ACCCO_RelatedAccount__c?.value;
  if (id) this.patientsFromPolygon.push(id);
});
