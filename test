resetLookupComponent() {
    // Forcer la réinitialisation du composant lookup
    const lookupComponent = this.template.querySelector('c-fsapp-lookup-filter');
    if (lookupComponent) {
        // Déclencher la réinitialisation du lookup
        lookupComponent.handleCommit();
    }
}
